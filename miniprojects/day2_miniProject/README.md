# Addis Eats - Typed & Filtered Menu (Day 2 Mini-Project)

A React project featuring PropTypes validation, default currency (ETB), guarded boolean spicy badge, reusable Card wrapper, and category filtering with early-return empty states and stable unique keys.

## How to run it
```bash
npm install
npm run dev
```
