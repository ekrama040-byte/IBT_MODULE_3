# Addis Eats - Self-Loading Menu (Day 4 Mini-Project)

Menu that fetches data from an endpoint using `AbortController`, handles loading/error states cleanly, refetches on category change, and focuses a search input on mount via `useRef`.

## How to run it
```bash
npm install
npm run dev
```
